@model BusinessFinancialAccounting.Models.ProfileDataViewModel
@{
    ViewData["Title"] = "Профіль";
}

<div class="d-block mx-auto my-3 p-3" style="max-width: 500px">
    <h2 class="text-center mb-3">Профіль користувача</h2>

    <h4>Персональні дані</h4>
    <form asp-action="UpdateProfile" method="post">
        <div class="mb-3">
            <label class="form-label">ПІБ</label>
            <input asp-for="FullName" class="form-control" />
            <span asp-validation-for="FullName" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label class="form-label">Телефон</label>
            <input asp-for="Phone" class="form-control" />
            <span asp-validation-for="Phone" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label class="form-label">Email</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-success w-100">Зберегти дані</button>

        <hr />

        <h4>Зміна паролю</h4>
        <a asp-action="ChangePassword" class="btn btn-warning w-100">Змінити пароль</a>

        <hr />

        <h4>Google</h4>
        @if (Model.IsGoogleLinked)
        {
            <p>Google акаунт прив’язаний.</p>
            <a asp-action="UnlinkGoogle" class="btn btn-danger w-100">Відв’язати Google</a>
        }
        else
        {
            <a asp-action="LinkGoogle" class="btn btn-primary w-100">Прив’язати Google</a>
        }

    </form>

</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}